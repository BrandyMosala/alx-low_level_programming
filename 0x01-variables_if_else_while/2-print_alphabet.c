#include <stdio.h>
/**
 *main - Entry point
 *Description: 'print the alphabet in lowercase'
 *Return: Always 0
 */
int main(void)
{
int num = 97;
while (n <= 122)
{
	putchar(num);
	num++;
}
return (0);
}
